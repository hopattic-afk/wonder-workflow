# Interactive homepage direction

Owner reference: https://ramp.com/ — inspected live September8 using browser and web source. The homepage pairs a clear proposition with animated product artifacts (invoice capture, expense/bill processing, layered desktop/mobile product UI). This is inspiration for communicating function, not permission to reuse its assets, claim its customers/results, or repeat an unverified ranking.

Wonder & Workflow implementation: replace the static hero emblem with a native interactive illustration of service-business operations. Three original examples show source information becoming an organized brief, then stop for human review before handoff. User-selectable scenarios and steps make the connection tangible. No real contacts, third-party requests, invented savings or live operational metrics. Clearly label the workflow illustrative.

Preserve the previous requested silent opening logo film, approved light palette, readable text, original assets, two-page assessment path, legacy console and CRM contracts. Run the workflow example once after the intro; stop at review; reduced motion uses immediate manual state changes. Scenario changes and reruns clear prior approval. A disabled Handoff control cannot bypass approval.

Acceptance: desktop/mobile layout, all three scenarios, keyboard actions, reset, approval boundary, reduced-motion/manual path, intro handoff, bounded timers, no background service writes. This demonstrates the proposed service approach, not a deployed client system.
