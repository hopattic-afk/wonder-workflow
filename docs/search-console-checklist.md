# Search Console checklist (Ian)

Do not invent traffic metrics. Wonder & Workflow is live at https://wonderworkflow.com. GA4 measurement ID `G-Q5FNTC3EXW` is already on the site.

Ian must click verify. Nobody else can complete Search Console ownership for this property.

1. Open https://search.google.com/search-console
2. Add property: URL prefix `https://wonderworkflow.com` (or Domain if you prefer DNS)
3. Verify (HTML tag / DNS / Google Analytics if the GA4 property is linked. GA4 Measurement ID `G-Q5FNTC3EXW` is live. An HTML meta tag or HTML file is also fine if that is cleaner.)
4. After verified: Sitemaps → submit `https://wonderworkflow.com/sitemap.xml`
5. Optional: URL Inspection on `/` and `/assessment`

Canonicals in this site use no trailing slash except the homepage `/`. Sitemap locs match that slash policy.
