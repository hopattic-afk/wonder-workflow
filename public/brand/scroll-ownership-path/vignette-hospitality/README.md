# Vignette variant: hospitality (rotate-only)

**Not the default Ownership path face.** Use only when the vignette rotate lands on Hospitality invoices.

Primary defaults live one level up: Office / Crew / Owner.

| File | Role |
|------|------|
| `role-tile-kitchen.svg` | Kitchen label tile |
| `role-tile-bar.svg` | Bar label tile |
| `role-tile-owner.svg` | Owner label tile (hospitality set twin) |

Same motion grammar (Stuck → Named → Moving). Same tokens. Swap labels/tiles only; do not replace the primary stage comps with this set as homepage default.
